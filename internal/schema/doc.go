// Package schema reads JSON Schema (draft 2020-12) documents and exposes
// the SQL primitives shared by the landing (flat table) and normalized
// (fully-tabular) pipelines: node parsing, type mapping, identifier
// mangling, SQL literals and payload value collection.
package schema
